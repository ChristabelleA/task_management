//Enum representing task priority levels in the task management system
// used for categorizing tasks based on their urgency
// add validation handling for task priority
namespace TaskManagementAPI.Enums
{
    public enum Priority
    {
        None = 0,
        Low = 1,
        Medium = 2,
        High = 3,
        Urgent = 4,
    }
}
